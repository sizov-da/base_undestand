Для создания виртуального окружения (venv) в Python на macOS, выполните следующие шаги:

1. Откройте терминал.
2. Перейдите в директорию вашего проекта.
3. Выполните команду для создания виртуального окружения:

```sh
python3 -m venv venv
```

4. Активируйте виртуальное окружение:

```sh
source venv/bin/activate
```

Теперь ваше виртуальное окружение активировано, и вы можете устанавливать зависимости, которые будут изолированы от глобальных пакетов Python.